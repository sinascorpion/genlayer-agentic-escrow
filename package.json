{
  "name": "genlayer-agentic-escrow",
  "version": "1.0.0",
  "description": "Agentic Escrow - Autonomous AI-mediated escrow primitive on GenLayer",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "test": "node tests/test_regression_suite.js",
    "test:lifecycle": "node tests/test_lifecycle.js"
  },
  "dependencies": {
    "genlayer-js": "^1.1.8"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
